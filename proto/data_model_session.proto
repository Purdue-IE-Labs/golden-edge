syntax = "proto3";

import "base_data.proto";

message DataModel {
    repeated DataItem data = 1;
}

message DataItem {
    oneof data {
        BaseData base_data = 1;
        DataModel model_data = 2;
    }
}