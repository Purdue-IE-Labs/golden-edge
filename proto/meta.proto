syntax = "proto3";

import "tag_data.proto";

message Property {
    DataType type = 7;
    TagData value = 8;
}

message Meta {
    // methods
        // parameters
        // return values
    message Method {
    }
    
    // tags
        // name
        // datatypes
        // properties
    message Tag {
        string name = 1;
        DataType type = 2;
        string key_expr = 3;
        map<string, Property> properties = 4;
    }

    repeated Tag tags = 5;
    repeated Method methods = 6;
}