syntax = "proto3";

import "tag_data.proto";

message Property {
    DataType type = 6;
    TagData value = 7;
}

message Meta {
    // methods
        // parameters
        // return values
    message Method {
    }
    
    // tags
        // name
        // datatypes
        // properties
    message Tag {
        string name = 1;
        DataType type = 2;
        map<string, Property> properties = 3;
    }

    repeated Tag tags = 4;
    repeated Method methods = 5;
}