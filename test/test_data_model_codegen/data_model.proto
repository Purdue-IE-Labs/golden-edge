syntax = "proto3";

message Object {
    string name = 1;
    repeated Object objects = 2;
    repeated Base bases = 3;
}

message Base {
    string name = 1;
    string type = 2;
}

message Model {
    string name = 1;
    repeated Object objects = 2;
    repeated Base bases = 3;
}